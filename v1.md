# RAG 后端 V1 功能与实现说明

## 1. 项目定位

这是一个基于 FastAPI 和 LlamaIndex 的本地 RAG 后端服务。当前版本已经具备用户认证、文档上传、文档摄取、向量化入库、混合检索、重排序、普通问答、SSE 流式问答、文档状态管理和会话列表管理等能力。

系统整体更接近“本地知识库问答服务原型 + 基础权限控制”，而不是完整生产级多租户知识库。当前已经有 `admin/user` 角色区分，但文档级权限、完整消息历史、多知识库隔离仍未实现。

## 2. 技术栈

### 2.1 Web 与接口

- FastAPI：API 服务框架。
- Pydantic：请求和响应模型。
- Uvicorn：服务运行入口。
- SSE：通过 `StreamingResponse` 实现流式输出。

### 2.2 RAG 框架

- LlamaIndex：文档摄取、索引、检索、Workflow 和回答合成。
- Chroma：本地向量数据库。
- Redis：LlamaIndex 的 docstore、index store 和 ingestion cache。
- SQLite：保存用户、文档状态和会话列表。

### 2.3 模型

- LLM：Ollama，默认模型为 `deepseek-r1:1.5b`。
- Embedding：`HuggingFaceEmbedding`，默认路径为 `D:\llm\Local_model\BAAI\bge-large-zh-v1___5`。
- Rerank：`SentenceTransformerRerank`，默认路径为 `D:\llm\Local_model\BAAI\bge-reranker-large`。

### 2.4 文档处理

- PDF 新流程：`pymupdf4llm` 将 PDF 转为 Markdown，并提取图片。
- 普通文件：`SimpleDirectoryReader` 读取。
- 旧版多模态 PDF 处理器仍保留在 `core/pdfProcessor.py`，但当前摄取流程实际使用的是 `core/pdf_parser.py`。

## 3. 项目目录

```text
rag/
├── app/
│   ├── main.py                  # FastAPI 应用入口
│   ├── schemas.py               # Pydantic 请求/响应模型
│   ├── routers/
│   │   ├── users.py             # 用户认证、注册、用户信息
│   │   ├── documents.py         # 文档上传、文档列表、系统 reset
│   │   └── chat.py              # 普通问答、流式问答、会话列表
│   └── services/
│       └── rag_service.py       # API 层到核心 RAG 应用的适配层
├── config/
│   └── settings.py              # 环境变量和默认配置
├── core/
│   ├── application.py           # RAG 应用编排层
│   ├── ingestion.py             # 文档摄取与向量化
│   ├── workflow.py              # RAG 检索、重排、生成工作流
│   ├── events.py                # Workflow 事件类型
│   ├── userManager.py           # SQLite 用户表管理
│   ├── documentManager.py       # SQLite 文档状态管理
│   ├── chatSessionManager.py    # SQLite 会话列表管理
│   ├── pdf_parser.py            # 当前使用的 PDF -> Markdown 解析器
│   └── pdfProcessor.py          # 旧版 PDF 多模态解析器，当前未接入主流程
├── utils/
│   └── logger.py                # 日志工具
├── file/
│   ├── app.db                   # SQLite 数据库
│   ├── chroma_db/               # Chroma 持久化目录
│   ├── resources/               # 上传后的原始文件
│   ├── image/                   # PDF 图片提取目录
│   └── storage_bm25/            # BM25 持久化目录
├── .env                         # 本地运行配置
├── .env.example                 # 配置模板
└── requirements.txt             # Python 依赖
```

## 4. 应用入口

入口文件是 `app/main.py`。

### 4.1 FastAPI 实例

```python
app = FastAPI(title="RAG API", version="1.0.0")
```

### 4.2 CORS

当前 CORS 配置允许所有来源、所有方法、所有请求头：

```python
allow_origins=["*"]
allow_credentials=True
allow_methods=["*"]
allow_headers=["*"]
```

这适合本地开发。生产环境应改成明确的前端域名白名单。

### 4.3 路由挂载

```text
/api/docs  -> app.routers.documents
/api/chat  -> app.routers.chat
/users     -> app.routers.users
```

健康检查：

```text
GET /api/health
```

返回：

```json
{
  "status": "ok",
  "message": "healthy"
}
```

## 5. 配置系统

配置集中在 `config/settings.py`，优先读取 `.env`，没有配置时使用默认值。

### 5.1 认证配置

```text
JWT_SECRET_KEY
JWT_ALGORITHM
ACCESS_TOKEN_EXPIRE_MINUTES
ENABLE_DEFAULT_ADMIN
DEFAULT_ADMIN_USERNAME
DEFAULT_ADMIN_PASSWORD
```

默认会创建管理员账号：

```text
username=root
password=root
role=admin
```

如果用户已经存在，不会覆盖密码；如果该用户不是 admin，会把角色修正为 admin。

### 5.2 LLM 配置

```text
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=deepseek-r1:1.5b
OLLAMA_REQUEST_TIMEOUT=600
MODEL=deepseek-r1:1.5b
TEMPERATURE=0.1
```

当前查询接口可以传 `model`，服务层会调用 `update_model_config()`，最终使用传入模型名或默认 `OLLAMA_MODEL` 更新 LlamaIndex 全局 `Settings.llm`。

### 5.3 本地模型配置

```text
EMBEDDING_MODEL_PATH
RERANK_MODEL_PATH
```

默认值是 Windows 本地模型路径。

### 5.4 文档切分和检索配置

```text
CHUNK_SIZE=512
CHUNK_OVERLAP=50
TITLE_EXTRACTOR_NODES=5
SIMILARITY_TOP_K=5
RERANK_TOP_K=3
SIMILARITY_CUTOFF=0.5
```

### 5.5 存储配置

```text
REDIS_HOST=127.0.0.1
REDIS_PORT=6380
REDIS_INDEX_NAMESPACE=redis_index
REDIS_DOCS_NAMESPACE=redis_docs
REDIS_CACHE_COLLECTION=redis_cache

CHROMA_COLLECTION=quickstart
CHROMA_PERSIST_DIR=file/chroma_db
BM25_PERSIST_DIR=file/storage_bm25
RESOURCES_DIR=file/resources
PDF_IMAGE_DIR=file/image
LOG_DIR=file
SQLITE_DB_PATH=file/app.db
```

相对路径会自动解析到项目根目录下。

## 6. 用户认证与权限

实现文件：

- `app/routers/users.py`
- `core/userManager.py`
- `app/schemas.py`

### 6.1 用户表

SQLite 表名：`users`

字段：

```text
id
username
hashed_password
email
full_name
role
disabled
created_at
updated_at
```

`role` 当前支持：

```text
admin
user
```

如果旧数据库没有 `role` 字段，`UserManager._init_db()` 会自动执行轻量迁移：

```sql
ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'user'
```

### 6.2 登录

接口：

```text
POST /users/token
```

请求格式使用 OAuth2 表单：

```text
username=root
password=root
```

处理流程：

1. 根据 username 从 SQLite 读取用户。
2. 使用 `pwdlib.PasswordHash` 校验密码。
3. 生成 JWT，payload 中写入 `sub=username` 和 `exp`。
4. 返回 access token。

响应结构：

```json
{
  "message": "login success",
  "access_token": "...",
  "token_type": "bearer",
  "username": "root"
}
```

### 6.3 当前用户

```text
GET /users/me
PUT /users/me
DELETE /users/me
POST /users/logout
```

`GET /users/me` 返回当前登录用户基础信息，不返回 `hashed_password`。

### 6.4 注册

```text
POST /users/register
```

注册用户默认为：

```text
role=user
disabled=false
```

当前注册接口没有限制为 admin-only。

### 6.5 管理员权限

`get_current_admin_user()` 会检查：

```python
current_user.role == "admin"
```

不满足时返回 403：

```text
Admin permission required
```

当前 admin-only 接口：

```text
GET  /users/all
POST /api/docs/upload
POST /api/docs/reset
```

## 7. 文档管理功能

实现文件：

- `app/routers/documents.py`
- `app/services/rag_service.py`
- `core/documentManager.py`
- `core/ingestion.py`

### 7.1 文档上传

接口：

```text
POST /api/docs/upload
```

权限：

```text
admin only
```

请求方式：

```text
multipart/form-data
字段名: files
支持多文件
```

处理流程：

1. FastAPI 接收 `UploadFile`。
2. 文件保存到 `Settings.RESOURCES_DIR`，默认 `file/resources`。
3. 如果文件名冲突，自动生成 `stem_1.ext`、`stem_2.ext`。
4. `RAGService` 将文档状态标记为 `processing`。
5. 调用 `RAGApplication.upload_and_process_files()`。
6. 进入 `DocumentIngestionPipeline.ingest_documents()`。
7. 摄取成功后，文档状态标记为 `completed`。
8. 摄取失败后，文档状态标记为 `failed`。

响应结构：

```json
{
  "status": "success",
  "message": "...",
  "processed_files": ["example.pdf"]
}
```

### 7.2 文档状态表

SQLite 表名：`documents`

字段：

```text
id
doc_id
file_name
file_path
file_type
file_size
status
message
node_count
error
created_at
updated_at
```

`doc_id` 规则：

```text
knowledge_base/{file_name}
```

文档状态：

```text
processing
completed
failed
```

### 7.3 文档列表

接口：

```text
GET /api/docs/list
```

权限：

```text
active user
```

返回来源：

```text
SQLite documents 表
```

不会从 Chroma 或 Redis 反查文档列表。

### 7.4 node_count 统计

上传完成后，`RAGService` 会根据返回的 `pipeline_nodes` 统计每个文件生成的节点数量：

```python
node.metadata["file_name"]
```

然后把每个文件自己的节点数写入 `documents.node_count`。

### 7.5 reset

接口：

```text
POST /api/docs/reset
```

权限：

```text
admin only
```

当前 reset 清理内容：

- Chroma 中的指定 collection。
- Redis 中匹配 namespace 的 key。
- BM25 持久化目录。
- 上传文件目录 `file/resources`。
- PDF 图片目录 `file/image`。
- SQLite `documents` 表。
- 内存中的 `ingestion_pipeline.index`。
- 内存中的 `workflow`。

当前 reset 不清理：

- SQLite `users` 表。
- SQLite `chat_sessions` 表。

也就是说，reset 是“清知识库”，不是“清用户和会话”。

## 8. 文档摄取与向量化

核心文件：

```text
core/ingestion.py
core/pdf_parser.py
```

核心类：

```python
DocumentIngestionPipeline
```

### 8.1 初始化流程

`DocumentIngestionPipeline.__init__()` 会依次执行：

1. `_setup_models()`
2. `_initialize_storage_components()`
3. `_create_pipelines()`
4. 初始化 `MultimodalPDFProcessor`
5. 创建 `StorageContext`

### 8.2 模型初始化

LLM：

```python
Settings.llm = Ollama(
    base_url=AppSettings.OLLAMA_BASE_URL,
    model=AppSettings.OLLAMA_MODEL,
    request_timeout=AppSettings.OLLAMA_REQUEST_TIMEOUT,
    temperature=AppSettings.TEMPERATURE,
)
```

Embedding：

```python
Settings.embed_model = HuggingFaceEmbedding(
    model_name=AppSettings.EMBEDDING_MODEL_PATH,
    device="cuda" if torch.cuda.is_available() else "cpu",
)
```

### 8.3 存储初始化

Redis：

```text
RedisIndexStore      -> namespace=redis_index
RedisDocumentStore   -> namespace=redis_docs
RedisKVStore cache   -> collection=redis_cache
```

Chroma：

```text
PersistentClient(Settings.CHROMA_PERSIST_DIR)
collection=Settings.CHROMA_COLLECTION
默认 collection=quickstart
```

StorageContext：

```python
StorageContext.from_defaults(
    vector_store=self.chroma_vector_store,
    docstore=self.redis_document_store,
    index_store=self.redis_index_store,
)
```

### 8.4 PDF 解析

当前主流程使用 `core/pdf_parser.py`。

PDF 处理流程：

1. 使用 `pymupdf4llm.to_markdown()` 将 PDF 转成 Markdown。
2. `write_images=True`，图片保存到 `Settings.PDF_IMAGE_DIR`。
3. 构造 LlamaIndex `Document`。
4. 设置稳定文档 ID：

```text
knowledge_base/{file_name}
```

5. metadata：

```python
{
    "file_name": file_name,
    "doc_id": stable_doc_id,
    "file_path": pdf_path,
    "content_type": "markdown"
}
```

### 8.5 普通文档解析

非 PDF 文件使用：

```python
SimpleDirectoryReader(input_files=[physical_path])
```

读取后会统一处理：

- 强制 `doc.id_ = knowledge_base/{file_name}`。
- 删除容易变化的 metadata：

```text
file_path
last_modified_date
```

- 写入稳定 metadata：

```text
file_name
doc_id
```

### 8.6 普通文本 Pipeline

普通文档使用 `text_pipeline`：

```text
Document
-> SentenceSplitter(chunk_size=512, chunk_overlap=50)
-> TitleExtractor
-> HuggingFaceEmbedding
-> Chroma / Redis / Cache
```

### 8.7 Markdown/PDF Pipeline

PDF 会先转为 Markdown，然后进入 `markdown_pipeline`：

```text
Markdown Document
-> MarkdownNodeParser(include_metadata=True, include_prev_next_rel=True)
-> SentenceSplitter(chunk_size=512, chunk_overlap=50, separator="\n\n")
-> TitleExtractor
-> HuggingFaceEmbedding
-> Chroma / Redis / Cache
```

使用 `separator="\n\n"` 和 `paragraph_separator="\n\n"`，目的是尽量按段落切分，减少 Markdown 图片语法被切断的概率。

### 8.8 索引创建与更新

Pipeline 生成 `pipeline_nodes` 后：

1. 显式写入 docstore：

```python
self.storage_context.docstore.add_documents(pipeline_nodes)
```

2. 如果当前没有 index：

```python
self.index = VectorStoreIndex(
    pipeline_nodes,
    storage_context=self.storage_context,
    embed_model=Settings.embed_model,
)
```

3. 如果已有 index：

```python
self.index.insert_nodes(pipeline_nodes)
```

4. 返回：

```text
status
message
pipeline_nodes
```

## 9. RAG 查询与生成

核心文件：

```text
core/application.py
core/workflow.py
core/events.py
```

### 9.1 查询入口

普通查询：

```text
POST /api/chat/chat
```

流式查询：

```text
POST /api/chat/chat/stream
```

请求体：

```json
{
  "session_id": null,
  "query": "问题内容",
  "model": "deepseek-r1:1.5b",
  "knowledge_bool": true,
  "temperature": 0.1,
  "max_tokens": 100
}
```

### 9.2 knowledge_bool 分支

`knowledge_bool=true`：

```text
走 RAG 检索 + 重排 + 生成
```

`knowledge_bool=false`：

```text
直接调用 Settings.llm
不走知识库检索
```

### 9.3 Workflow 初始化

`RAGApplication._ensure_workflow(streaming)` 会确保：

1. 如果 index 不存在，尝试通过 `ingestion_pipeline.get_documents()` 从存储加载。
2. 如果仍没有 index，抛出错误。
3. 如果 workflow 不存在，或 streaming 模式不一致，则重新创建 `RAGWorkflow`。

### 9.4 检索器

`RAGWorkflow._setup_components()` 初始化：

向量检索：

```python
VectorIndexRetriever(
    index=self.index,
    similarity_top_k=Settings.SIMILARITY_TOP_K
)
```

BM25：

```python
BM25Retriever.from_defaults(
    nodes=self.nodes,
    similarity_top_k=Settings.SIMILARITY_TOP_K,
    language="zh"
)
```

如果 BM25 可用，则使用融合检索：

```python
QueryFusionRetriever(
    retrievers=[vector_retriever, bm25_retriever],
    similarity_top_k=Settings.SIMILARITY_TOP_K,
    num_queries=1,
    mode="reciprocal_rerank",
    use_async=True,
)
```

如果 BM25 不可用，则降级为纯向量检索。

### 9.5 BM25 持久化

当本次上传产生新 `pipeline_nodes` 时：

```text
使用本次 nodes 构建 BM25
persist 到 Settings.BM25_PERSIST_DIR
```

服务重启或没有新 nodes 时：

```text
尝试从 Settings.BM25_PERSIST_DIR 加载 BM25
```

注意：当前 BM25 构建逻辑主要基于本次传入的 nodes，是否覆盖全量语料需要后续继续优化。

### 9.6 重排序

使用：

```python
SentenceTransformerRerank(
    model=Settings.RERANK_MODEL_PATH,
    top_n=Settings.RERANK_TOP_K
)
```

默认保留：

```text
RERANK_TOP_K=3
```

### 9.7 回答生成

Response Synthesizer：

```python
get_response_synthesizer(
    response_mode=ResponseMode.SIMPLE_SUMMARIZE,
    streaming=self.streaming
)
```

生成：

```python
await synthesizer.asynthesize(query=ev.query, nodes=ev.nodes)
```

### 9.8 Workflow 事件

定义在 `core/events.py`：

```text
RetrievalEvent: query + nodes
RerankEvent:    query + nodes
ResponseEvent:  query + nodes + response
```

Workflow 步骤：

```text
StartEvent
-> retrieve_step
-> rerank_step
-> generate_step
-> finalize_step
-> StopEvent
```

### 9.9 返回 sources

`finalize_step` 会把每个命中的 node 转为：

```python
{
    "content": node.node.text,
    "score": node.score,
    "metadata": node.node.metadata
}
```

`RAGApplication._format_sources()` 再把 source 格式化为前端可展示字符串。

如果 metadata 中 `content_type=markdown`，还会尝试从内容里提取图片路径并转 base64 展示。

## 10. 流式问答

接口：

```text
POST /api/chat/chat/stream
```

返回类型：

```text
text/event-stream
```

### 10.1 首包 session

服务层 `RAGService.query_stream()` 会先返回 session 信息：

```json
{
  "type": "session",
  "finished": false,
  "content": {
    "session_id": "...",
    "title": "..."
  }
}
```

### 10.2 RAG 模式流式响应

当 `knowledge_bool=true`：

1. 先检索和重排。
2. 如果有 sources，先返回：

```json
{
  "type": "sources",
  "finished": false,
  "content": []
}
```

3. 然后逐 token 返回：

```json
{
  "type": "text",
  "finished": false,
  "content": "增量文本"
}
```

4. 最后返回：

```json
{
  "type": "complete",
  "finished": true,
  "content": "完整回答"
}
```

### 10.3 非知识库模式流式响应

当 `knowledge_bool=false`：

```python
Settings.llm.astream_complete(query)
```

然后逐 token 返回 `text` 事件，最后返回 `complete`。

## 11. 会话管理

实现文件：

```text
core/chatSessionManager.py
app/routers/chat.py
app/services/rag_service.py
```

### 11.1 会话表

SQLite 表名：`chat_sessions`

字段：

```text
id
session_id
username
title
first_query
last_message
message_count
created_at
updated_at
```

### 11.2 创建和复用会话

`ChatSessionManager.ensure_session()` 逻辑：

1. 如果请求传入 `session_id` 且属于当前用户，复用会话。
2. 如果 `session_id` 存在但属于其他用户，则丢弃并新建。
3. 如果没有传 `session_id`，生成 UUID。
4. 标题取首次问题前 18 个字符。

### 11.3 更新会话

问答完成后调用：

```python
touch_session(
    username=username,
    session_id=session_id,
    last_message=query,
    message_increment=1,
)
```

会更新：

```text
last_message
message_count
updated_at
```

### 11.4 会话接口

```text
GET    /api/chat/sessions
DELETE /api/chat/sessions/{session_id}
POST   /api/chat/clear
GET    /api/chat/history
```

### 11.5 当前限制

当前只持久化“会话列表”，没有持久化完整消息内容。

`RAGApplication.get_session_history()` 当前返回空列表：

```python
return []
```

`RAGApplication.clear_session()` 当前也是空实现：

```python
return None
```

因此：

- `/api/chat/sessions` 可用。
- `/api/chat/history` 基本返回空。
- `/api/chat/clear` 不会真正清理消息历史，因为消息历史本身未落库。

## 12. 存储设计

### 12.1 SQLite

默认路径：

```text
file/app.db
```

保存：

```text
users
documents
chat_sessions
```

### 12.2 Chroma

默认路径：

```text
file/chroma_db
```

默认 collection：

```text
quickstart
```

保存：

```text
文本 chunk 的向量数据
```

### 12.3 Redis

默认连接：

```text
127.0.0.1:6380
```

命名空间：

```text
redis_index
redis_docs
redis_cache
```

用途：

- `RedisIndexStore`：索引结构。
- `RedisDocumentStore`：文档和 nodes。
- `RedisKVStore`：ingestion cache。

### 12.4 文件目录

```text
file/resources      上传后的原始文件
file/image          PDF 提取图片
file/storage_bm25   BM25 索引
file/logs           日志
```

## 13. 日志

实现文件：

```text
utils/logger.py
```

日志输出到：

```text
stdout
file/logs/{logger_name}.log
```

日志文件使用 UTF-8 编码。

## 14. API 汇总

### 14.1 健康检查

```text
GET /api/health
```

### 14.2 用户

```text
POST   /users/token       登录
POST   /users/register    注册
POST   /users/logout      登出
GET    /users/me          当前用户信息
PUT    /users/me          更新当前用户
GET    /users/protected   认证示例接口
GET    /users/all         用户列表，admin only
DELETE /users/me          删除当前用户
```

### 14.3 文档

```text
POST /api/docs/upload     上传并摄取文档，admin only
GET  /api/docs/list       文档状态列表
POST /api/docs/reset      清空知识库，admin only
```

### 14.4 问答

```text
POST   /api/chat/chat                 普通问答
POST   /api/chat/chat/stream          SSE 流式问答
GET    /api/chat/sessions             当前用户会话列表
DELETE /api/chat/sessions/{session_id} 删除会话
GET    /api/chat/history              当前用户历史，当前基本为空
POST   /api/chat/clear                清空会话，当前基本为空实现
```

## 15. 当前已实现能力清单

- JWT 登录认证。
- 密码哈希存储。
- 默认管理员自动初始化。
- 用户角色字段。
- admin-only 文档上传。
- admin-only 系统 reset。
- admin-only 用户列表。
- 文档上传到本地资源目录。
- 文档状态入 SQLite。
- PDF 转 Markdown。
- PDF 图片提取。
- 普通文档读取。
- 文档稳定 ID。
- 普通文本切分。
- Markdown 结构解析和段落切分。
- 本地 embedding。
- Chroma 向量入库。
- Redis docstore/index/cache。
- VectorStoreIndex 创建和增量插入。
- BM25 检索。
- Vector + BM25 融合检索。
- SentenceTransformer rerank。
- LlamaIndex response synthesizer。
- 普通问答。
- SSE 流式问答。
- source 返回。
- 会话列表落库。
- 文档 node_count 统计。
- reset 清理知识库相关存储。

## 16. 当前限制和待优化点

### 16.1 文档权限未实现

当前没有文档级 `owner_id`、`access_level`、`department` 等 metadata，也没有检索前权限过滤。

### 16.2 会话消息未落库

当前只有会话列表，没有完整消息表。无法真正恢复某个会话的聊天内容。

### 16.3 BM25 全量一致性仍需确认

当前 BM25 在上传后主要基于本次上传的 nodes 构建并持久化。它是否覆盖全量知识库，需要后续根据实际行为继续优化。

### 16.4 reset 不清用户和会话

当前 reset 只清知识库和文档状态，保留用户和会话列表。这是当前设计选择。

### 16.5 CORS 过宽

当前允许所有来源，生产环境需要改为白名单。

### 16.6 注册接口未限制

当前任何人都可以注册普通用户。生产环境可以考虑改为 admin 创建用户或关闭开放注册。

### 16.7 多模型支持较浅

接口可以传 `model`，但当前主要针对 Ollama 模型切换，DashScope 代码仍处于注释/残留状态。

### 16.8 部分历史代码未接入主流程

`core/pdfProcessor.py` 旧版多模态 PDF 处理器仍保留，但当前主摄取流程使用 `core/pdf_parser.py`。

## 17. V1 总结

V1 已经形成完整的本地 RAG 闭环：

```text
登录 -> 上传文档 -> PDF/文本解析 -> 切分 -> embedding -> Chroma/Redis 入库
-> 向量 + BM25 混合检索 -> rerank -> LLM 生成 -> 普通/流式返回
```

同时具备基本的 admin 权限控制和 SQLite 状态管理。

下一阶段最建议优先补齐：

1. 文档级权限控制。
2. 检索时 metadata filter。
3. 完整聊天消息落库。
4. BM25 全量重建或增量一致性策略。
5. 管理员后台接口和审计日志。
